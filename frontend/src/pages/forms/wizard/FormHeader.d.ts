import React from 'react';
interface FormHeaderProps {
    title: string;
    description: string;
}
declare const FormHeader: ({ title, description }: FormHeaderProps) => React.JSX.Element;
export default FormHeader;
